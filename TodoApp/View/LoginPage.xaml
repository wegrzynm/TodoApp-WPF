<Page x:Class="TodoApp.View.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:TodoApp.View"
      xmlns:controller="clr-namespace:TodoApp.Controller"
      mc:Ignorable="d"
      Title="LoginPage" Height="450" Width="300">
    <Page.DataContext>
        <controller:LoginController/>
    </Page.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25*"/>
            <ColumnDefinition Width="75*"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock>Login: </TextBlock>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="Username" Text="{Binding Username}"/>
       
        <TextBlock Grid.Row="1">Password: </TextBlock>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="Password" Text="{Binding Password}"/>
       
        <Button Grid.Row="2" Grid.ColumnSpan="2" x:Name="LogInButton" Command="{Binding LoginCommand}">Log in!</Button>
    </Grid>
</Page>
